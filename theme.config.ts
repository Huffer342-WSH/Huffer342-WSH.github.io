import { defineThemeConfig } from 'valaxy-theme-yun/node'
export default defineThemeConfig({
  type: 'strato',

    banner: {
        enable: false,
      title: '',
        cloud: {
          enable: false,
        },
      },

 
})
